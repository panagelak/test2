<launch>
  <arg name="config" default="perception"/>
  <!-- launch rviz-->
  <node name="$(anon rviz)" pkg="rviz" type="rviz" respawn="false"
    output="screen" args="-d $(find perception_gazebo)/rviz/$(arg config).rviz"/>

</launch>